#include <stdio.h>

int main()
{
    int N, sum = 0, i;

    if (scanf("%d", &N) != 1)
    {
        return 1;
    }

    i = 1;
    while (i <= N)
    {
        sum = sum + i;
        i = i + 1;
    }

    printf("%d\n", sum);

    return 0;
}
